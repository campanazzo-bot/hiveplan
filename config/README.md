# OpenClaw Config Snapshot (Sanitized)

This folder contains structure-only snapshots of local OpenClaw configuration.
All secrets/tokens are redacted.

- `openclaw-structure.json`: sanitized `~/.openclaw/openclaw.json`
- `lead-coordinator-mcp.json`: MCP wiring structure for `lead-coordinator`
